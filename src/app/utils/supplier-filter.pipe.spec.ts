import { SupplierFilterPipe } from './supplier-filter.pipe';

describe('SupplierFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SupplierFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
